# Prompt Library

Each prompt follows the C-C-C-R-V model.

A production prompt should make these explicit:

- Context
- Constraints
- Command
- Result
- Validation

Prompts are versioned by behavior, not by length. When changing a prompt, record:

1. the observed failure
2. the hypothesis
3. the change
4. the evaluation result
