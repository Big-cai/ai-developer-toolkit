# Skills

A Skill is a reusable capability package, not merely a prompt.

A Skill should define:

```text
Trigger
Input
Context
Rules
Workflow
Output
Validation
Failure handling
```

The toolkit uses `SKILL.md` as the human-readable contract.
